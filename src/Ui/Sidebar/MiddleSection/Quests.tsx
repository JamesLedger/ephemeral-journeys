import React from "react"
import { Canvas } from "@react-three/fiber"
import { styled } from "styled-components"

const QuestItem = styled.div`
  width: 400px;
  height: 55vh;
  background: red;
`

const Quests = () => {
  return <QuestItem>Armour</QuestItem>
}

export default Quests
